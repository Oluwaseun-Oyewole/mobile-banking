import MainWrapper from "@/components/main/wrapper";
import { CustomText } from "@/components/text";
import React from "react";
import { View } from "react-native";

const Interest = () => {
  return (
    <MainWrapper>
      <View>
        <CustomText>Interest</CustomText>
      </View>
    </MainWrapper>
  );
};

export default Interest;
