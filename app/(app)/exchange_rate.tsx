import MainWrapper from "@/components/main/wrapper";
import { CustomText } from "@/components/text";
import React from "react";
import { View } from "react-native";

const ExchangeRate = () => {
  return (
    <MainWrapper>
      <View>
        <CustomText>ExchangeRate</CustomText>
      </View>
    </MainWrapper>
  );
};

export default ExchangeRate;
