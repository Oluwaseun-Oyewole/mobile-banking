import MainWrapper from "@/components/main/wrapper";
import { CustomText } from "@/components/text";
import React from "react";

const Withdrawal = () => {
  return (
    <MainWrapper>
      <CustomText>Withdrawal</CustomText>
    </MainWrapper>
  );
};

export default Withdrawal;
